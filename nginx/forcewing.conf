# Define the parameters for a specific virtual host/server
server {
    # Define the directory where the contents being requested are stored
    # root /usr/src/app/project/;

    # Define the default page that will be served If no page was requested
    # (ie. if www.kennedyfamilyrecipes.com is requested)
    # index index.html;

    # Define the server name, IP address, and/or port of the server
    listen 80;
    # server_name xxx.yyy.zzz.aaa
    # listen [::]:80;
    # Define the specified charset to the “Content-Type” response header field
    charset utf-8;

    # server_name forcewing.com www.forcewing.com
    # Configure NGINX to deliver static content from the specified folder
    location /static {
        alias /usr/src/app/forcewing/static;
    }

    # Configure NGINX to reverse proxy HTTP requests to the upstream server (Gunicorn (WSGI server))
    location / {
        # Define the location of the proxy server to send the request to
        proxy_pass http://web:8000;

        # Redefine the header fields that NGINX sends to the upstream server
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        # Define the maximum file size on file uploads
        client_max_body_size 5M;
        client_body_buffer_size 5M;
    }

    # location ^~ /.well-known{
    #     allow all;
    #     root /data/letsencrypt/;
    # }
}

# server {
#     listen      443           ssl http2;
#     listen [::]:443           ssl http2;
#     server_name               forcewing.com www.forcewing.com;

#     ssl                       on;

#     add_header                Strict-Transport-Security "max-age=31536000" always;

#     ssl_session_cache         shared:SSL:20m;
#     ssl_session_timeout       10m;

#     ssl_protocols             TLSv1 TLSv1.1 TLSv1.2;
#     ssl_prefer_server_ciphers on;
#     ssl_ciphers               "ECDH+AESGCM:ECDH+AES256:ECDH+AES128:!ADH:!AECDH:!MD5;";

#     ssl_stapling              on;
#     ssl_stapling_verify       on;
#     resolver                  8.8.8.8 8.8.4.4;

#     ssl_certificate           /etc/letsencrypt/live/forcewing.com/fullchain.pem;
#     ssl_certificate_key       /etc/letsencrypt/live/forcewing.com/privkey.pem;
#     ssl_trusted_certificate   /etc/letsencrypt/live/forcewing.com/chain.pem;

#     access_log                /dev/stdout;
#     error_log                 /dev/stderr info;

#     # other configs
# }